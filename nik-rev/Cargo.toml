[package]
name = "nik-rev"
version = "0.1.4"
edition = "2015"
license = "MIT OR Apache-2.0"
description = "generates markdown badges in build script, for use in crate-level documentation"

[dependencies]
